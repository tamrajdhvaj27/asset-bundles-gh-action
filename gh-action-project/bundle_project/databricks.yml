# This is a Databricks asset bundle definition for bundle_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle_project
  uuid: 36f24853-9f13-4b77-8055-eead12c461f7

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f6ab2eda-6073.cloud.databricks.com
    variables:
      catalog: dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-f6ab2eda-6073.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/tamraj.dhvaj@zohomail.in to make sure we only have a single copy.
      root_path: /Workspace/Users/tamraj.dhvaj@zohomail.in/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod
    permissions:
      - user_name: tamraj.dhvaj@zohomail.in
        level: CAN_MANAGE
